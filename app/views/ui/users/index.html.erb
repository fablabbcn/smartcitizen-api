<h2>User information</h2>

<% if current_user %>
  <p>Logged in as <%= current_user.email %>. </p>
  <p>Your access token:
  <br /><code><%= current_user.access_token.token %></code></p>
  <p><%= link_to "Log out", logout_path %></p>
<% else %>
  <p>Not logged in!</p>
  <a href="<%= new_ui_session_path %>">Go back</a>
<% end %>
